# Early embedded text diagnostics

Unmodified v1 offscreen-fixture diagnostics from the first embedded text implementation. The first comparison
exceeded investigation bands; later functional-only runs are explicitly unpaired. These files cannot validate
the independent v2 scene or the complete AV plugin. Final matching v2 measurements belong to DxUi's TextInput
measurement archive. No threshold or raw round was changed during retention.
